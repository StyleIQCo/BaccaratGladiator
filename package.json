{
  "name": "baccarat-gladiator",
  "version": "1.0.0",
  "description": "Live baccarat arena with all 5 casino roads, EZ & Tiger modes, AI bots, and global leaderboards.",
  "scripts": {
    "cap:sync": "npx cap sync",
    "cap:ios": "npx cap open ios",
    "cap:android": "npx cap open android",
    "cap:build:ios": "npx cap sync ios && npx cap copy ios",
    "cap:build:android": "npx cap sync android && npx cap copy android"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.2",
    "@capacitor/core": "^7.6.2",
    "@capacitor/ios": "^7.6.2",
    "@capacitor/splash-screen": "^7.0.5",
    "@capacitor/status-bar": "^7.0.6",
    "firebase": "^12.12.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.2"
  }
}
