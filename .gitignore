.DS_Store
Thumbs.db
*.swp
*.swo
node_modules/

# Runtime config — contains API endpoints, never commit
config.js

# Android keystore — never commit
*.keystore
*.jks
google-services.json

# iOS secrets
AuthKey_*.p8

# Personal / sensitive documents — never commit
Apple_Developer*
.claude/

# Build artifacts — regenerated by `sam build` / Python; never commit
.aws-sam/
__pycache__/

# Marketing / promo media — kept locally, not version-controlled (repo bloat)
# NOTE: deliberately NOT ignoring all *.mp4 — dealer/ videos and clipN.mp4 are
# game assets referenced by deployed pages and must stay tracked.
/shorts/
/baccarat-gladiator-*.mp4
/dragon7_clip.mp4
/baccarat-gladiator-itchio.zip

# Vite build output (arena web)
arena/web/dist/

# Race demo single-file build output
arena/web/dist-race-demo

# Odyssey smoke harness build output
odyssey/smoke/dist/
