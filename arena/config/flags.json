{
  "_comment": "ROLLBACK KILL SWITCH. Set enabled:false and deploy ONLY this file to instantly take the arena offline without touching the classic game. The web shell reads this before connecting any WebSocket. demoMode:true renders the mock-driven Demo Hub instead of the live table (no backend needed) — flip to false once the gateway is hosted.",
  "enabled": true,
  "demoMode": true,
  "maintenanceMessage": "The Grand Arena is taking a short break — back soon.",
  "classicGameUrl": "/",
  "features": {
    "crashSideBet": true,
    "rainBounty": true,
    "chat": true,
    "video": true
  },
  "limits": {
    "minConnectIntervalMs": 250,
    "cashOutBucketTokens": 5,
    "cashOutBucketRefillMs": 1000
  }
}
